module.exports = function (sim, simDetails) {
    if (simDetails.company)
        sim.company = simDetails.company;
    if (simDetails.company)
        sim.company = simDetails.company;
    if (simDetails.company)
        sim.company = simDetails.company;
    if (simDetails.company)
        sim.company = simDetails.company;
    if (simDetails.company)
        sim.company = simDetails.company;

    return sim;

}